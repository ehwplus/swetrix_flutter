name: swetrix_flutter
description: A lightweight Swetrix Events API client for Flutter and Dart apps.
version: 0.1.0
homepage: https://swetrix.com
repository: https://github.com/Swetrix/swetrix-dart
issue_tracker: https://github.com/Swetrix/swetrix-dart/issues

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  http: ^1.2.1
  meta: ^1.12.0

dev_dependencies:
  flutter:
    sdk: flutter
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # This package does not bundle assets or fonts by default.
